name: memora_mobile
description: Memora mobile app connected to Laravel API.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.2
  shared_preferences: ^2.3.2
  url_launcher: ^6.3.0
  flutter_svg: ^2.0.10+1
  google_sign_in: ^6.2.1
  flutter_lucide: ^1.11.0
  lucide_icons: ^0.257.0
  get_it: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true